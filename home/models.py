from django.db import models
CATEGORY = (('vector', 'vector'), ('raster', 'raster'), ('ui', 'ui'), ('printing', 'printing'))
STATUS = (('active', 'active'), ('', 'default'))

# Create your models here.
